package com.pilates.app.controls.listeners;

import com.pilates.app.controls.IconButton;
import com.pilates.app.controls.RoundButton;

public interface OnButtonClickListener {
    void clicked(IconButton button);
}
